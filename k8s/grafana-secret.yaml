apiVersion: v1
kind: Secret
metadata:
  name: grafana-api-secret
  namespace: traceassist
type: Opaque
stringData:
  # IMPORTANT: Replace the placeholder below with the actual Grafana token you just created
  GRAFANA_API_TOKEN: "" 
